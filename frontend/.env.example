# Copy to .env for local development. Points the frontend dev server at the
# FastAPI demo backend (see ../video_captioning/app.py -- run it with
# `uvicorn app:app --reload --port 8000`). Not needed in production if the
# frontend is served from the same origin as the API.
VITE_API_BASE_URL=http://localhost:8000
